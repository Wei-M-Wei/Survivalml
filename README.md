It is the first version of R package 'survivalml', which does not contain the raw dataset which was used in this paper. We build a dropbox link to allow you to get access to the dataset. The processed sub-datasets are also in this link.
We would suggest you download this project to install our package 'survivalml', then load by 'library(survivalml)'.
CRAN package will come soon.
